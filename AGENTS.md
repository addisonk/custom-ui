# AGENTS.md

Read the shared machine setup checklist first:

`/Users/addisonkowalski/Dropbox/.agents/docs/agent-machine-setup.md`

## Project

`addisonk/custom-ui` is a personal collection of composable UI components built
on shadcn/ui conventions and distributed through registry JSON URLs.

## Working In This Repo

- Keep component docs and registry entries in sync.
- Preserve shadcn-style install commands and component examples in `README.md`.
- Do not commit generated local app output, screenshots unless intentional, or
  machine-specific cache files.

## Services And Env

Non-secret service identity notes live in `.agent-context/infra.md`.

No runtime service identity is detected from committed config in this checkout.
